const config = {
    preset: 'jest-expo',
    verbose: true,
    roots: [
        "<rootDir>"
    ],
    modulePaths: [
        "./src"
    ],
    moduleNameMapper: {

    },

}

module.exports = config